import UserForm from "../components/forms/UserForm";

const Login = () => {
    return (
        <main>
            <h1>Connexion</h1>
            <UserForm page="login" />
        </main>
    );
};

export default Login;
