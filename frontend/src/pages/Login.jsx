import UserForm from "../components/forms/UserForm";

const Login = () => {
    return (
        <main>
            <h1>Log in</h1>
            <UserForm page="login" />
        </main>
    );
};

export default Login;
